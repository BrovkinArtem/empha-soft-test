# EmphaSoft - User Management System

Single Page Application (SPA) для управления пользователями с системой аутентификации, фильтрацией, сортировкой и адаптивным дизайном.

## 📋 Описание проекта

Приложение представляет собой полнофункциональную систему управления пользователями с следующими возможностями:

- **Аутентификация пользователей** с валидацией данных
- **Отображение списка пользователей** с фильтрацией по username и сортировкой по ID
- **Создание и редактирование пользователей** через модальные окна
- **Адаптивный дизайн** для всех типов устройств (мобильные, планшеты, десктопы)
- **Защищенные маршруты** с проверкой авторизации

## 🚀 Функциональность

### Аутентификация
- Форма входа с валидацией полей
- Хранение токена в HTTP-only cookies для защиты от XSS
- Автоматическая проверка авторизации при загрузке приложения
- Защищенные маршруты с редиректом на страницу входа

### Управление пользователями
- Просмотр списка всех пользователей
- Фильтрация по username с debounce (задержка 500ms)
- Сортировка по ID (по возрастанию/убыванию/без сортировки)
- Создание нового пользователя через модальное окно
- Редактирование существующего пользователя через модальное окно
- Валидация всех полей формы при создании/редактировании

### UI/UX
- Адаптивный дизайн для всех размеров экранов
- Модальные окна для создания/редактирования пользователей
- Индикаторы загрузки
- Обработка и отображение ошибок
- Плавные анимации и переходы

## 🛠 Используемые технологии и инструменты

### Frontend Framework
- **React 18** - библиотека для построения пользовательского интерфейса
- **Vite** - быстрый сборщик и dev-сервер

### State Management
- **Redux Toolkit (RTK)** - управление глобальным состоянием приложения
- **RTK Query** - работа с API, кэширование и инвалидация данных

### Routing
- **React Router DOM v6** - маршрутизация с поддержкой nested routes и protected routes

### Forms & Validation
- **React Hook Form** - управление формами с оптимизацией производительности
- **Zod** - валидация схем данных с TypeScript-подобным синтаксисом

### Styling
- **CSS Modules** - модульные стили для компонентов
- **CSS Variables** - централизованное управление темами
- **Media Queries** - адаптивный дизайн

### Utilities
- **js-cookie** - работа с cookies для хранения токена
- **Custom Hooks** - переиспользуемая логика (useDebounce, useLocalStorage)

### Development Tools
- **ESLint** - линтинг кода
- **Path Aliases** - упрощение импортов (@/components, @/store и т.д.)

## 📁 Структура проекта

```
src/
├── components/          # Переиспользуемые UI компоненты
│   ├── ui/            # Базовые компоненты (Button, Input, Modal)
│   ├── layout/        # Компоненты макета (Container)
│   └── common/        # Общие компоненты (Loading)
├── features/           # Функциональные модули
│   ├── auth/          # Модуль аутентификации
│   └── users/          # Модуль управления пользователями
├── pages/              # Страницы приложения
│   ├── auth/          # Страницы аутентификации
│   └── users/         # Страницы пользователей
├── store/              # Redux store
│   ├── api/           # RTK Query API slice
│   └── slices/        # Redux slices
├── routes/             # Конфигурация маршрутизации
├── utils/              # Утилиты
│   ├── cookies.js     # Работа с cookies
│   └── validation/    # Схемы валидации (Zod)
├── hooks/              # Custom React hooks
└── styles/             # Глобальные стили
    ├── variables.css  # CSS переменные
    ├── reset.css      # Сброс стилей
    └── media.css      # Медиа-запросы
```

## 🎯 Проделанная работа

### 1. Настройка проекта
- Создан React проект через Vite
- Настроены path aliases для упрощения импортов
- Настроен прокси для API запросов (обход CORS)
- Настроена структура папок по feature-based архитектуре

### 2. Аутентификация
- Реализована форма входа с валидацией через React Hook Form + Zod
- Настроена работа с API через RTK Query
- Реализовано хранение токена в HTTP-only cookies
- Созданы protected routes с проверкой авторизации
- Реализована автоматическая проверка авторизации при загрузке

### 3. Управление пользователями
- Реализован список пользователей с загрузкой данных через RTK Query
- Добавлена фильтрация по username с debounce
- Добавлена сортировка по ID (asc/desc/null)
- Созданы формы создания/редактирования пользователей
- Реализована валидация всех полей формы
- Интегрированы модальные окна для создания/редактирования

### 4. UI/UX
- Созданы переиспользуемые UI компоненты (Button, Input, Modal, Loading)
- Реализован адаптивный дизайн для всех устройств
- Добавлены индикаторы загрузки
- Реализована обработка и отображение ошибок
- Добавлены плавные анимации для модальных окон

### 5. Оптимизация и качество кода
- Использованы React hooks для оптимизации (useMemo, useCallback)
- Реализовано кэширование данных через RTK Query
- Добавлена инвалидация кэша при создании/обновлении пользователей
- Очищен код от лишних комментариев и console.log
- Настроен линтинг кода

## 🔧 Установка и запуск

### Требования
- Node.js 16+ 
- npm или yarn

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для продакшена
```bash
npm run build
```

### Предпросмотр продакшен сборки
```bash
npm run preview
```

## 🔐 Учетные данные для входа

- **Username:** `test_super`
- **Password:** `Nf<U4f<rDbtDxAPn`

## 📡 API

Приложение использует API по адресу: `https://test-assignment.emphasoft.com/api/v1/`

Все запросы проксируются через Vite dev-сервер для обхода CORS ограничений.

## 🎨 Адаптивность

Приложение полностью адаптивно и поддерживает следующие breakpoints:

- **Mobile:** до 767px
- **Tablet:** 768px - 1023px
- **Desktop:** 1024px+
- **Large Desktop:** 1440px+

## 🚧 Что можно улучшить

### Функциональность
1. **Удаление пользователей** - добавить возможность удаления пользователей с подтверждением
2. **Пагинация** - реализовать пагинацию для больших списков пользователей
3. **Поиск по нескольким полям** - расширить фильтрацию (по имени, фамилии, статусу)
4. **Экспорт данных** - добавить возможность экспорта списка пользователей в CSV/Excel
5. **Массовые операции** - добавить возможность массового редактирования/удаления

### UX/UI
1. **Toast уведомления** - добавить систему уведомлений об успешных операциях
2. **Skeleton loading** - заменить индикаторы загрузки на skeleton screens
3. **Темная тема** - добавить поддержку темной темы
4. **Локализация** - добавить поддержку нескольких языков (i18n)
5. **Accessibility** - улучшить доступность (ARIA атрибуты, keyboard navigation)

### Технические улучшения
1. **TypeScript** - мигрировать проект на TypeScript для типобезопасности
2. **Unit тесты** - добавить unit тесты для компонентов и утилит (Jest + React Testing Library)
3. **E2E тесты** - добавить end-to-end тесты (Cypress или Playwright)
4. **Error Boundary** - добавить Error Boundary для обработки ошибок React
5. **Оптимизация производительности** - добавить React.memo, lazy loading для компонентов
6. **PWA** - превратить приложение в Progressive Web App
7. **Офлайн режим** - добавить поддержку работы в офлайн режиме через Service Workers

### Безопасность
1. **Rate limiting** - добавить ограничение количества запросов
2. **CSRF защита** - улучшить защиту от CSRF атак
3. **Валидация на сервере** - убедиться, что все валидации дублируются на сервере
4. **Content Security Policy** - настроить CSP заголовки

### DevOps
1. **CI/CD** - настроить автоматическую сборку и деплой
2. **Docker** - добавить Docker контейнеризацию
3. **Мониторинг** - интегрировать системы мониторинга (Sentry, LogRocket)
4. **Аналитика** - добавить аналитику использования (Google Analytics)

## 📝 Лицензия

Этот проект создан в рамках тестового задания.

## 👤 Автор

Проект разработан для EmphaSoft test assignment.
